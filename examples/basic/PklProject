amends "pkl:Project"

dependencies {
  // Reference local plugin schema during development
  ["example"] = import("../../schema/pkl/PklProject")

  // Formae schema - fetched from public registry
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.77.2-internal"
  }
}
